#split the array program(same allocate program code)
#painters partition(same program od allocate books)

#stacks & queues:-
stack=[]
print(type(stack))
stack.append(10)
print(stack)
stack.append(20)
print(stack)
print(stack.pop()) # or print(stack[-1])[-1] in the -1 used as a peak element
